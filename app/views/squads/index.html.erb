All Squads

<% @squads.each do |squad| %>
  <p>
    Squad name:
    <%= squad.name %>
  </p>
  <p>
    Squad formation:
    <%= squad.formation %>
  </p>
<% end %>

<%= link_to "new suqad", new_squad_path %>